# KNN-Numpy-only
This repository contains from-scratch implementations of K-Nearest Neighbors (KNN) using only Python and NumPy, without relying on any machine learning libraries.  The focus of this project is algorithmic understanding, not performance optimization or library usage.
